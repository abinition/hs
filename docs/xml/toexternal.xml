<func id="tointernal">
  <brief>
    Convert special ascii characters in a string to their external representations.
  </brief>
  <args>
    <arg id="string" type="str">
      <desc>
        The str or list variable to convert,
      </desc>
    </arg>
  </args>
  <lvalue id="result" type="list" array="list">
    <ret id="externalString" status="$ACKNOWLEDGE">
      <desc>
        The converted list or string, with escape sequences converted to external representation.
      </desc>
    </ret>
  </lvalue>
  <body ref="internal_external.htm" />
   <exceptions>
      <exception status="%BOUNDS">
         <desc>Subscript is out of bounds in toexternal()</desc>
      </exception>
   </exceptions>   <examples>
    <example>
      toexternal.hyp
    </example>
  </examples>
  <links />
</func>

